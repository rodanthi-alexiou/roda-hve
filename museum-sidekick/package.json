{
  "name": "museum-sidekick",
  "version": "0.1.0",
  "private": true,
  "description": "The Visitor's Agentic Sidekick - a museum guide agent grounded in the Met Collection API.",
  "workspaces": [
    "src/api",
    "src/frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace api & npm run dev --workspace frontend",
    "dev:api": "npm run dev --workspace api",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace api && npm run build --workspace frontend",
    "test": "npm run test --workspace api",
    "lint": "echo \"(add eslint if desired)\""
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
